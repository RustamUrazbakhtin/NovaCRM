import PlaceholderTemplate from "./PlaceholderTemplate";

export default function TasksPage() {
    return (
        <PlaceholderTemplate
            breadcrumb="Tasks"
            title="Tasks"
            description="Coordinate daily work, assign follow-ups, and keep every teammate aligned with shared to-do lists."
        />
    );
}
