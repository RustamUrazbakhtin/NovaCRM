import PlaceholderTemplate from "./PlaceholderTemplate";

export default function ReviewsPage() {
    return (
        <PlaceholderTemplate
            breadcrumb="Reviews"
            title="Reviews"
            description="Collect testimonials, respond instantly, and celebrate service quality across every channel."
        />
    );
}
