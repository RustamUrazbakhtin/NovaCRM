import PlaceholderTemplate from "./PlaceholderTemplate";

export default function InventoryPage() {
    return (
        <PlaceholderTemplate
            breadcrumb="Inventory"
            title="Inventory"
            description="Track stock levels, automate purchase orders, and keep every product ready for the next appointment."
        />
    );
}
