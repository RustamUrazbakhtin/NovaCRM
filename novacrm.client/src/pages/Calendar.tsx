import PlaceholderTemplate from "./PlaceholderTemplate";

export default function CalendarPage() {
    return (
        <PlaceholderTemplate
            breadcrumb="Calendar"
            title="Calendar"
            description="Stay on top of bookings, staff shifts, and resource utilization with a real-time schedule view."
        />
    );
}
